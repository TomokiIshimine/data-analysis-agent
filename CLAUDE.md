# CLAUDE.md

## プロジェクトの目的

Claude Code 上で再現性重視のデータ分析を一気通貫で行うワークフロー `/data-analysis` を提供する。アプリケーションコードはなく、`.claude/` 配下のワークフロースキル 1 本・サブエージェント 9 体・How スキル 4 本が真実の源泉である。ドキュメントはそれらの地図であり、内容を再掲しない。

## 設計原則

- **自己完結**: 外部プラグインのサブエージェント・スキルを参照しない。動く条件をリポジトリ内に閉じる。
- **DRY → 最小コンテキスト → 疎結合**（この優先順）: 情報の集約先は 1 箇所。必要なコンテキストだけをロードする。サブエージェントは命名規約を知らず、出力先は引数で受け取る。
- **再現性最優先**: 元データは既定で複製せず参照と SHA-256 照合で同一性を担保し、実行資産は単体で再実行できる形で残す。
- 各原則の根拠と捨てた代替案は `docs/design-decisions.md` に記録する。

## 規約の要点

- 本文は日本語、識別子・ファイル名は英語ケバブケース。
- 永続ファイル内の参照パスはプロジェクトルート相対。実行時にエージェント間で渡すパスは絶対パス。
- サブエージェントは `AskUserQuestion` を呼ばず、成果物の絶対パス 1 行だけを返す。
- `tasks/` は揮発系の作業ディレクトリで Git 管理外。`src/` や `docs/` には成果物を書かない。
- 資産を追加・改修したら対応するドキュメントを同じ作業で更新する。詳細は `docs/conventions.md`。

## ディレクトリ構成

```
.claude/skills/data-analysis/SKILL.md   ワークフロー本体。手順・命名規約・限定 Read 契約を一元保持
.claude/skills/<how-skill>/SKILL.md     How スキル 4 本（データ取り扱い・設計サイクル・実行記録・HTML デザイン）
.claude/agents/*.md                     サブエージェント 9 体
docs/                                   地図・判断記録・規約
tasks/<yyyy-mm-dd>_data-analysis/       実行ごとの作業ディレクトリ（Git 管理外）
```

## ドキュメント

- `docs/architecture.md` — 3 層構造、7 ステップ、サブエージェントと How スキルの役割と依存関係
- `docs/design-decisions.md` — 設計判断の決定・根拠・代替案・影響範囲
- `docs/conventions.md` — 資産の書き方、共通契約、ドキュメント更新義務、Git 管理
- `README.md` — 利用者向けの使い方
