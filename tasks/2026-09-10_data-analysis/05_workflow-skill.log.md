# ワークフロースキル生成ログ — data-analysis

- 生成日時: 2026-09-10
- 入力設計書: `/Users/tishimine/Workspace/data-analysis-agent/tasks/2026-09-10_data-analysis/02_workflow-design.md`
- ターゲットプロジェクトルート: `/Users/tishimine/Workspace/data-analysis-agent`
- モード: 新規生成（`review-report-path` / `improvement-plan-path` は渡されていない）

## 生成したファイル

| # | 絶対パス | 種別 |
|---|---|---|
| 1 | `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/data-analysis/SKILL.md` | ワークフロースキル |
| 2 | `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/analysis-design-cycle/SKILL.md` | How スキル |
| 3 | `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/html-deliverable-design/SKILL.md` | How スキル |
| 4 | `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/analysis-run-recording/SKILL.md` | How スキル |
| 5 | `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/analysis-data-handling/SKILL.md` | How スキル |

## ワークフロースキルの frontmatter 最終形

```yaml
name: data-analysis
description: 分析要件の定義から反復分析、再現可能な Markdown レポート化、第三者視点の再現検証、HTML 清書までを一気通貫で行うワークフロー。手元のデータについて分析目的はあるが手順が固まっていないとき、または分析結果を他者に共有するために「同じ手順で同じ結果が出る」ことの担保が必要なときに、ユーザーが `/data-analysis` と明示的に打って起動する。単発の集計やグラフ 1 枚の作成には使わない。
argument-hint: "<analysis-request — 分析の目的・対象データの所在（ファイルパス／ディレクトリ／DB 接続／URL）・知りたいことを自然文で>"
disable-model-invocation: true
```

### frontmatter の自己点検結果

- **`allowed-tools` が含まれていないこと**: 含まれていない。設計書 `## 生成されるワークフロースキル` の「`allowed-tools` は指定しない（導入先のツール事情に依存させないため）」に一致。
- **`disable-model-invocation` が設計書記載どおりの値であること**: 設計書 `## 生成されるワークフロースキル` の `- disable-model-invocation: true` を転写した。既定値の付与はしていない。
- **`- 選択理由:` 行の存在**: 設計書 381 行目に記載あり（「コード実行・多数のファイル生成・複数の承認ゲートを伴う重量級ワークフローであり、会話中の『データ分析』という語での誤発火コストが利便性を上回るため、明示呼び出し専用とする。」）。整合チェック合格。本文 `## 実行前提` にこの値と根拠を明記した。
- **`argument-hint`**: 設計書 382 行目の記載をそのまま転写。
- **`description`**: 明示呼び出し専用のため自動発火はしないが、いつこのスキルを使うべきか（トリガー条件）と、使わない場面（単発集計）を明示した。
- **本文に `## 共通契約への準拠` 節があること**: あり。ただし参照先は設計書の指示により外部プラグインスキルではない（下記「設計書と /context-engineering の衝突」を参照）。

## 本文見出し一覧（ワークフロースキル）

- `# data-analysis — 再現性重視のデータ分析ワークフロー`
- `## 目的`
- `## 実行前提`
- `## 共通契約への準拠`
  - `### メインの作法`
  - `### 設計サイクル契約への準拠`
- `## 限定 Read 契約（固定見出しテーブル）`
- `## 中間生成物の命名規約`
  - `### 実行資産ディレクトリ（runs/<run-id>/）の内訳`
- `## 実行手順`
  - `### Step 1 — 作業ディレクトリ準備`
  - `### Step 2 — データ取り込みとプロファイリング`
  - `### Step 3 — 分析要件定義（draft → 承認ゲート → revise 反復）`
  - `### Step 4 — 分析ループ（plan → execute → evaluate、最大 N 周）`
  - `### Step 5 — 再現可能レポート作成（Markdown）`
  - `### Step 6 — 再現実験ループ（最大 M 周）`
  - `### Step 7 — レポート清書（HTML）`
- `## 最終報告`
- `## 失敗時のリカバリ`

設計書 384 行目の「本文に含める節」（前提 / メインの作法 / 設計サイクル契約への準拠 / 限定 Read 契約の固定見出しテーブル / 中間生成物の命名規約 / 実行手順 / 最終報告 / 失敗時のリカバリ）を全数カバーしている。「メインの作法」と「設計サイクル契約への準拠」は `## 共通契約への準拠` 配下の H3 として配置した（節名の要件と、本 author の `## 共通契約への準拠` 節配置要件を同時に満たすため）。

## 設計書との対応根拠

| SKILL.md の記述 | 設計書の根拠箇所 |
|---|---|
| ワークフロー名 `data-analysis`、配置 `.claude/skills/data-analysis/SKILL.md` | `## 生成されるワークフロースキル`（379, 383 行目） |
| 目的・利用シーン | `## 目的と利用シーン`（68〜78 行目） |
| Step 1〜7 の委譲先・引数・戻り値 | `## 全体手順（Step 1〜N）`（80〜155 行目） |
| Step 3 承認時に取り出す 5 値（確定データソース／機密区分／完了条件／`N`／`M`） | 108 行目 |
| Step 3-4 データソース再解決の分岐条件 | 109 行目、197〜199 行目（シーケンス図） |
| Step 4-4 / Step 6-2 のループ制御（`SATISFIED`/`CONTINUE`、`REPRODUCED`/`MISMATCH`、上限到達時も次 Step へ進む） | 127 行目、144 行目 |
| Step 6-3（`MISMATCH` 時のみ `analysis-report-author` 再起動、`verification-report-path` 追加） | 145〜148 行目 |
| 反復上限の既定値（`N`=5、`M`=3、要件シートで上書き可） | 65〜66 行目 |
| 限定 Read 契約の固定見出しテーブル（4 行） | `### メインの限定 Read 契約`（366〜375 行目） |
| 中間生成物の命名規約テーブル、`runs/<run-id>/` の内訳、規約箇条書き | `## 中間生成物の配置とファイル名規約`（318〜364 行目） |
| 「命名規約を知るのは SKILL.md だけ」「サブは出力先絶対パスを引数で受け取る」 | 320 行目 |
| 最終報告の項目 | `### 最終報告`（157〜159 行目） |
| 外部プラグイン非参照・自己完結 | 57〜58 行目、310〜316 行目、486 行目、488 行目 |
| 全ステップ直列（並列実行なし） | `## シーケンス図`（163〜234 行目）に並列 spawn は存在しない |
| How スキル 4 本の内容 | `## 生成される How スキル`（388〜458 行目）の各「定めるもの」「定めないもの」 |
| How スキルの `disable-model-invocation` 未指定 | 390 行目（「`disable-model-invocation` は未指定とする」） |

「失敗時のリカバリ」は設計書 384 行目で節の存在が指示されているが、具体的な手順は設計書に記載がない。設計書の判断（整合チェック失敗時に自動補正・自動進行しない、成果物を削除しない、承認なしに進まない）と各ステップの入出力から、各 Step の失敗事象ごとの対応を導出して記述した。設計書との矛盾はない。

## 設計書と /context-engineering の衝突と、その処理

**衝突 1: `## 共通契約への準拠` 節の参照先**

- 本 author の既定規約は、`## 共通契約への準拠` 節で外部プラグインの `/workflow-orchestration`（メインの横断的な作法）および `/design-cycle`（設計サイクルと承認ゲート）を **参照のみで完結** させ、内容を再掲しないことである。
- 一方、設計書は次を明示的に要求している。
  - 384 行目「メインの作法（サブエージェントへの絶対パス引き回し、限定 Read の遵守、ユーザー対話はメインのみが担うこと。**外部プラグインのスキルを参照せず、本節で完結させる**）」
  - 384 行目「設計サイクル契約への準拠（**プロジェクト内 How スキル `analysis-design-cycle` を参照し再掲しない**）」
  - 488 行目「メインの作法（絶対パス引き回し・限定 Read・ユーザー対話の一本化）は SKILL.md 本文に直接書く（1 箇所にしか現れないため、スキルへ切り出さない）。**外部プラグインが提供するスキル・サブエージェントは、生成物のいかなる箇所からも参照しない**。」
  - 460〜472 行目 `## ⚠️ /context-engineering からの逸脱` 逸脱 1（ユーザーの明示要求「プラグイン資産の流用はやめて下さい。別環境で自己完結したいです。」による、承認済みの逸脱）
- **優先順位（ワークフロー構成設計書 → /context-engineering）に従い、設計書を採用した。**
  - `## 共通契約への準拠` 節は残したうえで、`### メインの作法` に横断的な作法（責務分担・絶対パス引き回し・限定 Read・ユーザー対話の一本化と `ToolSearch` 1 回ロード・サブエージェント実行モード・戻り値形式・整合チェック失敗時の中断ポリシー・メインがしないこと）を **本文に直接記述** した。`/workflow-orchestration` への参照は書いていない。
  - `### 設計サイクル契約への準拠` は、`/design-cycle` ではなく **プロジェクト内 How スキル `analysis-design-cycle`** を参照し、2 モード契約・承認ゲート手順・revise の同期要件を **再掲しない** 旨を明記した。本ワークフロー固有の差分（要約セクションの実体が `<section id="design-summary">` であること、承認時に取り出す値、承認後のデータソース再解決）は Step 3 に置いた。
- 参照先を差し替えるにあたり、記述の齟齬を避けるため `/workflow-orchestration` と `/design-cycle` の本体を作業開始前に読み込み、内容を写経ではなく本ワークフローの語彙・ステップ番号に合わせて書き下ろした。契約の実質（限定 Read 契約の 3 カラム規約と本文全読禁止、絶対パスの `<…>` 記法、戻り値は絶対パス 1 行、承認ゲートの 6 手順、反復上限なし、メインがしないこと）は保持している。

**衝突 2: なし**（それ以外の判断は /context-engineering と設計書で矛盾しなかった）

- How スキル 4 本の切り出しは、/context-engineering `references/skills.md`「複数サブエージェント／複数ワークフローから呼び出される汎用的な How → スキルとして切り出す（DRY 原則）」に一致（`analysis-design-cycle` 2 者・`html-deliverable-design` 2 体・`analysis-run-recording` 4 体・`analysis-data-handling` 5 体）。
- ワークフロースキル本文を「手順と呼び出し構成」中心にし、How の詳細（実行資産の保存構成・データ取り扱い・HTML の作り・設計サイクル）を How スキルへ委譲したのは、同ファイル「ワークフロースキルは手順と呼び出し構成の記述が中心。How の詳細は呼び出す How スキル側に委ねる」に一致。
- ファイルパス記法: 永続ファイル本文中の参照はプロジェクトルート相対（`data/manifest.md` 等）、実行時にエージェント間で引き回すパスは絶対パス（`<workdir>/...`）とし、/context-engineering `## ファイルパスの記法` の例外規定に従った。

## How スキル 4 本の frontmatter 自己点検

いずれも `name` と `description`（いつ参照すべきかを明示）のみを持ち、`allowed-tools` は指定していない。`disable-model-invocation` は設計書 390 行目「未指定とする（サブエージェントが `Skill` ツールで明示呼び出しするため、無効化しない）」に従い、**キー自体を frontmatter に置いていない**。

| スキル名 | 定めるものの網羅 | 定めないものの明記 |
|---|---|---|
| `analysis-design-cycle` | 設計書 399〜404 行目の 5 項目すべて（2 モード入出力／AskUserQuestion を呼ばない／推奨値で埋め根拠を要約に明示／revise の同期と「前回からの主な変更点」／メインの承認ゲート手順／前提破綻時の中断） | 405 行目に一致（要件シートの見出し構成・HTML の見た目） |
| `html-deliverable-design` | 設計書 415〜421 行目の 7 項目すべて（単一ファイル完結／情報の優先順位と見出し階層／タイポグラフィ・余白・行長／配色／表・コード・図表／アクセシビリティ下限／文書種別による強調対象の違い） | 422 行目に一致（テンプレート・コンポーネントライブラリ・ブランド色）。固定テンプレートを提供していないことを本文冒頭で明言 |
| `analysis-run-recording` | 設計書 432〜440 行目の 9 項目すべて（run-dir 構成と `<NN>_<slug>` 命名／必ずファイル保存してから実行／run.sh の要件／environment.md の項目とデータの識別子参照／derived の扱い／再実行で変わる要素の固定／失敗の記録／認証情報／run-manifest.md の索引） | 441 行目に一致（言語・ライブラリ・手法の選択、データの台帳と検証と機密） |
| `analysis-data-handling` | 設計書 451〜457 行目の 7 項目すべて（データソースの解決と UNRESOLVED／manifest.md の記録項目／取り込み方式の判定／同一性の検証／機密区分と可否／認証情報／レポートへの転記） | 458 行目に一致（実行資産の保存構成、分析手法、機密区分の具体値） |

DRY の担保: `environment.md`（実行環境）と `data/manifest.md`（データ）の記録項目が重複しないよう、`analysis-run-recording` 側は「入力データは `data/manifest.md` のエントリを識別子で参照し、内容を再掲しない」と明記し、`analysis-data-handling` 側は実行環境の記録項目を持たせていない（設計書 479 行目の指示に一致）。

## 未対応・後続工程への申し送り

- 本 author の担当は SKILL.md（ワークフロースキル 1 本 ＋ How スキル 4 本）のみ。`## 作成対象サブエージェント` の 9 体（`/Users/tishimine/Workspace/data-analysis-agent/.claude/agents/*.md`）は別工程の担当であり、本ログの時点では未生成。
- レビュー工程での照合点: SKILL.md 本文に外部プラグイン提供のスキル名（`/workflow-orchestration` `/design-cycle` `/user-hearing` `/multi-aspect-review` `/context-engineering`）およびサブエージェント名（`workflow-init` 等）が現れないこと。上記の衝突処理により、生成した 5 ファイルのいずれにもこれらの参照は含まれていない。

---

## 再生成ログ（analysis-data-handling / iteration 1）

- 担当ファイル: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/analysis-data-handling/SKILL.md`
- 再生成対象レポート: `06_review-1_context-engineering.md`
- 対応した指摘件数: 1 件
  - 重大度 中 / 箇所: `## 5. 機密区分` の箇条書き（「掲載可否の判断に迷う場合は載せない」の項）および `## 7. レポートへの転記` の冒頭
    - 対応内容: `## 5. 機密区分` の当該箇条書きから、ワークフロー固有の成果物ファイル名 `04_analysis-report.md` / `06_analysis-report.html` の直書きを削除し、役割名「再現可能レポート（Markdown）と清書レポート（HTML）」に置き換えた。これにより本 How スキルがワークフロースキルの命名規約（番号接頭辞を含むファイル名）に従属しなくなり、疎結合原則と「命名規約を知るのは `data-analysis/SKILL.md` だけ」という契約に整合する。
    - `## 7. レポートへの転記` の冒頭は既に役割名表記であり、レポートの「期待される状態」どおり変更不要と判断（表現の到達点として当該箇所に揃えた）。
    - 変更範囲は当該 1 行のみ。`data/manifest.md` / `data/raw/` / `data/snapshot/` は設計書が本スキルの「定めるもの」として明示規定しているため、レポートの指示どおり変更対象外とした。
- 未対応として残した指摘: なし（本レポート内で担当ファイル絶対パスが一致する指摘は上記 1 件のみ。他ファイル宛の指摘は抽出対象外として無視した）
- 設計書と /context-engineering の衝突: なし（今回の修正は設計書に根拠のない独自追加の除去であり、設計書の規定と衝突しない）
- 自己点検: frontmatter は無変更（`name` / `description` のみ。`allowed-tools` は不在、`disable-model-invocation` も設計書記載どおり不在）。本ファイルは How スキルのため、ワークフロースキル向けの本文構成規約（Step 手順・`## 共通契約への準拠` 節）は非適用。

---

## 再生成ログ（iteration 1）

- 再生成対象レポート: `06_review-1_context-engineering.md`
- 担当ファイル: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/data-analysis/SKILL.md`
- レポート内で担当ファイルを対象とする指摘: 1 件（重大度: 軽微 / 箇所: `### 実行資産ディレクトリ（runs/<run-id>/）の内訳` のテーブル 7 行）
- 対応した指摘件数: 1 件
- 未対応として残した指摘: なし

### 対応内容

- `### 実行資産ディレクトリ（`runs/<run-id>/`）の内訳` を `### 実行資産ディレクトリ（`runs/<run-id>/`）` に改題し、`<run-id>` 配下の内訳テーブル 7 行を削除した。
- 代わりに、メインが組み立てるのは `runs/profile/` `runs/analysis-<n>/` `runs/repro-<m>/` の 3 系統までであることを明示し、配下の内訳・命名規約・要件は How スキル `analysis-run-recording` が唯一の情報源として定める旨の委譲記述に置き換えた。
- 指摘されていない箇所（frontmatter・共通契約への準拠・限定 Read 契約テーブル・命名規約の主テーブル・規約の箇条書き・実行手順 Step 1〜7・最終報告・失敗時のリカバリ）は変更していない。

### 設計書との整合判断

- 設計書 `## 生成されるワークフロースキル` の「本文に含める節」は「中間生成物の命名規約（`data/` と `runs/` の構成を含む）」を要求している。本修正後も `runs/` の構成（メインが組み立てる 3 系統）は主テーブルと本節に残っているため、設計書の要求は満たされている。設計書が要求しているのは `runs/` の構成であって `<run-id>` 配下の内訳の再掲ではないと解釈した。
- したがって「設計書 → /context-engineering」の優先順位に衝突はない。設計書の指示を維持したまま、レポートが指摘した DRY / 最小コンテキストの逸脱（`analysis-run-recording` が定める内訳の二重定義）のみを解消した。

### frontmatter 自己点検（再確認）

- `allowed-tools` は含まれていない。
- `disable-model-invocation: true` は設計書 `## 生成されるワークフロースキル` の記載どおり。今回変更なし。
- 本文に `## 共通契約への準拠` 節があり、設計書の承認済み逸脱 1（プロジェクト内自己完結）に従って、プロジェクト内 How スキル `analysis-design-cycle` を参照している。今回変更なし。

---

## 再生成ログ（analysis-data-handling / 周回 2）

- 再生成対象レポート: `06_review-2_prompt-engineering.md`
- 担当ファイル: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/analysis-data-handling/SKILL.md`
- 対応した指摘件数: 1 件

### 対応内容

- 重大度: 中 / 箇所: `## 5. 機密区分` の区分表「社内限定」行および直後の「**マスキング・集約の要件**」
  - 「十分な件数」「少数の個体」という読み手判断に委ねる語を廃し、既定の下限件数を数値（5 件）で固定した。
  - 箇条書きを「**マスキングの要件**」（機密区分の列の落とし方・不可逆変換）と「**集約の下限件数**」（掲載可否の閾値）に分割し、閾値規則を 1 箇所に集約。
  - 下限件数は要件シートで上書き可能である旨と、判定できない場合は掲載しないという既定を明記し、読み手が推測せず一意に判定できる形にした。
  - 二重記載の解消: 区分表「社内限定」行の「必要な処理」セルは「下記「集約の下限件数」を満たすこと」への参照に留め、要件本体は本文 1 箇所のみとした。

### 未対応として残した指摘

- なし（本レポートの他の指摘 7 件はいずれも対象ファイルが `data-analysis/SKILL.md` および `agents/*.md` であり、本 author の担当ファイル絶対パスと一致しないため対象外）。

### 自己点検

- frontmatter: `allowed-tools` を含まない（変更なし。How スキルのため `disable-model-invocation` は不使用）。
- 本文構成は How スキル（手法・契約のリファレンス）のまま維持。指摘されていない節（1〜4, 6, 7 および「本スキルが定めないもの」）は未変更。


---

## 再生成ログ（iteration 2）

- 再生成日時: 2026-09-10T19:35:38+0900
- 再生成対象レポート: `06_review-2_machine-readable.md`
- 担当ファイル: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/data-analysis/SKILL.md`
- 抽出方法: レポート `## 未解消の指摘 / 新規指摘` 配下から「対象ファイル」が担当ファイル絶対パスと一致する指摘のみ抽出。他 2 件（`data-ingestion-profiler.md` / `analysis-report-author.md`）は担当外のため読まず無視。
- 対応した指摘件数: 1 件（重大度: 中 — ハッシュ不一致をメインが検知する機械可読な経路が存在しない／同一事象に 2 通りの戻り値プロトコルが並立）

### 対応内容（レポートの選択肢 (A) を採用。限定 Read 契約テーブルは設計書どおり 4 行のまま維持）

1. `## 共通契約への準拠` → `### メインの作法` → 「サブエージェント戻り値の形式」
   - 「絶対パス 1 行のみ」を **正常終了時** の規約と明示し、整合チェック失敗時は **絶対パスを返さず** 不整合内容の報告のみを返す唯一の例外であること、メインは「戻り値が絶対パス 1 行でないこと」で中断を検知することを追記。
2. 同節「整合チェック失敗時の中断ポリシー」
   - 「編集系ツールを一切呼ばず、成果物ファイルも書かず、**絶対パスを返さずに** 不整合内容（対象・期待値・実測値を含む）を報告して終了する」に統一。あわせて「不整合の検知経路は本プロトコルに一本化し、成果物ファイルへの記録を介した検知経路は持たない」と明記して 2 通りのプロトコル並立を解消。
3. `## 失敗時のリカバリ` の「Step 4-2 で台帳とデータのハッシュが不一致」行
   - 「`analysis-executor` は実行ログを書かず絶対パスも返さないため、メインは戻り値が絶対パス 1 行でないことで検知する」「`analysis-executor` が戻り値として報告した不整合内容（対象・期待値・実測値）をそのままユーザーに提示し `AskUserQuestion` で選ばせる」に書き換え、限定 Read 契約テーブル 5 行目を追加せずに情報取得経路を成立させた。

### 併せて整合させた箇所（担当ファイル内・限定 Read 契約違反の解消）

- `## 失敗時のリカバリ` の「Step 2 でデータの取得自体に失敗（接続不可・ファイル不在）」行
  - 旧文面は「`01_data-profile.md` に記録された失敗内容を提示し」としており、メインの限定 Read 対象が同ファイルの `## データソース` のみである契約と矛盾していた（本レポートの `data-ingestion-profiler` 宛て指摘でも同じ矛盾が根拠として挙げられている）。取得失敗ソースを `## データソース` に `UNRESOLVED`（失敗理由付き）として記録する前提に揃え、メインは同セクションの限定 Read で検知して Step 3 承認ゲート → 3-4 再解決で復旧する、と書き換えた。担当ファイル内の記述のみを変更しており、他ファイルは編集していない。

### 未対応として残した指摘・残課題

- 未対応の指摘: なし（担当ファイル宛て 1 件はすべて対応済み）。
- 残課題（担当外ファイル、次周回で別インスタンスの対応が必要）: レポートの選択肢 (A) は `analysis-executor`（判断基準「ハッシュ不一致は停止条件」・作業手順 3 および 6/7・`## 出力`）と `analysis-data-handling` §4 を「実行ログを Write せず、対象・期待値・実測値を報告して終了する（絶対パスを返さない）」へ揃えることも求めているが、両ファイルは本レポートの「対象ファイル」に列挙されておらず、本再生成の担当範囲外のため未変更。現状 `analysis-executor.md` は不一致時も実行ログを Write し絶対パスを返す旨を残しており、SKILL.md 側の単一プロトコルと食い違うため、次周回で当該ファイル宛ての指摘として扱われる想定。

### frontmatter 自己点検（再確認）

- `allowed-tools` は含まれていない。
- `disable-model-invocation: true` は設計書 `## 生成されるワークフロースキル` の記載どおり。今回の再生成で frontmatter は変更していない。
- 本文に `## 共通契約への準拠` 節があり、プロジェクト内 How スキル `analysis-design-cycle`（設計サイクル契約）への委譲を明記している（本ワークフローはプラグイン外部の `/workflow-orchestration` `/design-cycle` に依存しない自己完結構成であり、メインの作法は同節に直接記述する方針を設計書が採用している）。

---

## 再生成ログ（3 周目）

- **再生成対象レポート**: `06_review-3_design-fidelity.md`
- **担当ファイル**: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/data-analysis/SKILL.md`（この 1 本のみ。How スキル 4 本・agents 9 本は触っていない）
- **対応した指摘件数**: 3 件（重大 1 / 軽微 2）。レポートの `## 未解消の指摘 / 新規指摘` から対象ファイルが担当ファイル絶対パスと一致する指摘のみ抽出した（3 件すべて一致）。
- **未対応として残した指摘**: なし。

### 対応内訳

1. **重大 — `## 共通契約への準拠` → `### メインの作法` の「整合チェック失敗時の中断ポリシー」および `## 失敗時のリカバリ` の Step 4-2 行**
   - 衝突解決: **設計書優先**。設計書 `## 作成対象サブエージェント` の `analysis-executor`（判断基準「ハッシュが台帳と一致しない場合は分析を進めず、不一致の事実を実行ログに記録して終了する」＋ 出力「`<execution-output-path>` を Write。最終メッセージは同絶対パス 1 行のみ」）を正とし、SKILL.md 側を戻した。設計書および他の生成物（`analysis-executor.md` / `analysis-data-handling`）は変更していない。
   - 変更したセクション:
     - 「サブエージェント戻り値の形式」— 絶対パスを返さない例外を「成果物を書かずに中断する経路」に限定。
     - 「整合チェック失敗時の中断ポリシー」— 停止条件の列挙から「台帳とデータのハッシュが一致しない」を外し、「不整合の検知経路は本プロトコルに一本化し、成果物ファイルへの記録を介した検知経路は持たない」という言い切りを削除。
     - 新設「不整合を成果物に記録して終了する経路（上記の例外）」— `analysis-executor` のハッシュ不一致（`<workdir>/03_iterations/<n>_execution.md` に記録して絶対パスを返す）と `data-ingestion-profiler` の `UNRESOLVED`（`<workdir>/01_data-profile.md` の `## データソース` に記録して絶対パスを返す）の 2 経路を例外として明示。
     - `## 失敗時のリカバリ` の Step 4-2 行 — 「実行ログを書かず絶対パスも返さない」前提を撤回し、実行ログを書いて絶対パスを返す前提に書き直した。到達不能だった `AskUserQuestion` による検知は撤去し、`analysis-evaluator` が要件逸脱として点検する設計書どおりの経路（設計書 `analysis-evaluator` 判断基準の「ハッシュ不一致のまま進めた実行」）に接続。上限到達時の最終報告に当該周回の `<n>_execution.md` / `<n>_evaluation.md` の絶対パスを含める形でユーザーへの復旧導線を確保した。
     - `## 最終報告` — 上限到達時のみ添えるパスを 2 行追加（上記との整合のため。設計書 `### 最終報告` の必須項目は削っていない）。
2. **軽微 — `### Step 6` の `6-2. ループ制御` 第 3 分岐**: 上限到達周回でも 6-3 を実行する解釈の根拠（最終周の差異をレポートに残さないまま清書へ渡さないため）を 1 行添えた。設計書 Step 6 の項番 2・3 は変更していない。
3. **軽微 — `3-4. データソース再解決（分岐）` の引数**: 設計書 Step 2 の入力欄に合わせて `analysis-request` を列挙し、再起動時は `data-source` が唯一の正である旨を併記した。

### frontmatter 自己点検（変更なし）

- `allowed-tools` は含まれていない。
- `disable-model-invocation: true` は設計書 `## 生成されるワークフロースキル` の記載どおり。
- 本文に `## 共通契約への準拠` 節があり、設計サイクル契約はプロジェクト内 How スキル `analysis-design-cycle` を参照している（設計書の自己完結方針により、外部プラグインの `/workflow-orchestration` `/design-cycle` は参照しない）。

---

## 再生成ログ（周回 3 / 観点 prompt-engineering）

- **再生成対象レポート**: `06_review-3_prompt-engineering.md`
- **担当ファイル**: `/Users/tishimine/Workspace/data-analysis-agent/.claude/skills/data-analysis/SKILL.md`
- **抽出方法**: `## 未解消の指摘 / 新規指摘` 配下から「対象ファイル」が担当ファイル絶対パスと一致する指摘のみ抽出（4 件）。他 4 件（`analysis-planner.md` / `data-ingestion-profiler.md` / `analysis-executor.md` 宛）は担当外につき読まない。
- **対応した指摘件数**: 4 件（中 2 件／軽微 2 件）
- **未対応として残した指摘**: なし

### 対応内訳

| 重大度 | 箇所 | 対応 |
|---|---|---|
| 中 | `**3-4. データソース再解決（分岐）**` | 「すなわち」による言い換えと末尾の否定形の再掲を削除し、発火条件を番号付き 2 条件（(1) `## データソース` が `UNRESOLVED`、(2) 承認された確定データソースが Step 2 の解決結果と異なる）として 1 度だけ列挙。非該当時の挙動は冒頭 1 文に統合。引数も 1 引数 1 行に展開。 |
| 中 | 各ステップの `**引数**:` 行（3-1 / 4-1 / 4-2 / 4-3 / Step 5 / 6-1 / 6-3 / Step 7 の 8 箇所） | Step 2 と同じ 1 引数 1 行の入れ子箇条書きに統一。区切り記号としての `/` を廃止。条件付き引数は行頭側に条件を出す形へ（`previous-evaluation-path` は「**2 周目以降のみ**」、`verification-report-path` は「**反映モードの指定**」＋ Step 5 の初回起動では渡さない旨）。あわせて 3-2 の散文中の `/` 区切りも読点へ変更。 |
| 軽微 | `### メインの作法`「限定 Read の遵守」「メインがしないこと」、`## 限定 Read 契約` 前文 | 制約の本体（本文全読禁止＋テーブル外を読みたくなった場合の解釈）を `## 限定 Read 契約` に 1 度だけ集約。「限定 Read の遵守」は同節への参照のみに短縮し、「メインがしないこと」は「限定 Read 契約に反する Read」と参照表現に置換。 |
| 軽微 | `**4-4. ループ制御**` の 3 番目の分岐 | Step 6-2 の書式に揃え、「`CONTINUE` かつ `n = N` → ループを抜けて Step 5 へ。終了理由は「上限到達」」と条件式に判定語を含める形へ修正。`SATISFIED` かつ `n = N` の二重該当を解消。 |

### 保全確認

- 直前の design-fidelity 対応で復元された「ハッシュ不一致時の中断ポリシー」を維持していること: `### メインの作法` の「不整合を成果物に記録して終了する経路（上記の例外）」（`analysis-executor` が不一致の事実を `<workdir>/03_iterations/<n>_execution.md` に記録して終了し同絶対パスを返す）と、`## 失敗時のリカバリ` の Step 4-2 行は **未変更**。今周回の編集は文体・書式・重複整理のみで、契約の意味を変更していない。
- frontmatter 自己点検: `allowed-tools` を含まない。`disable-model-invocation: true`（設計書記載どおり）。本節構成（`## 共通契約への準拠` とその配下の `### メインの作法` / `### 設計サイクル契約への準拠`）を維持。いずれも今周回で変更していない。
