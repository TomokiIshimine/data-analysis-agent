# 生成ログ: analysis-evaluator

- 生成先: `/Users/tishimine/Workspace/data-analysis-agent/.claude/agents/analysis-evaluator.md`
- 設計書: `tasks/2026-09-10_data-analysis/02_workflow-design.md`
- モード: 新規生成（`improvement-plan-path` / `review-report-path` いずれも未指定）

## frontmatter のキー一覧

- `name`: `analysis-evaluator`
- `description`: 設計書の担当ブロック `description` をそのまま採用
- `model`: `opus`
- `color`: `yellow`

自己点検:

- `tools` を含んでいない（導入先プロジェクトのツール事情に縛られないため、意図的に未指定）。
- `color` を含んでいる（未指定ではない）。

## `color` の判定根拠

採用値: `yellow`（レビュー・検証カテゴリ）。

判定根拠は担当ブロックの責務・出力記述に現れる次の表現。

- 責務: 「充足度を**判定**」「要件の範囲から逸脱していないかを**点検**」
- 出力: 「`## 判定` セクションに `SATISFIED` / `CONTINUE` のいずれか 1 語のみを置く」＝ 二値の合否判定を返す契約

境界例の検討: 本係は `<evaluation-output-path>` を Write するため「workdir Write」の側面も持つが、書き出すのは判定レポートであり、生成・適用（ターゲット配下の実体ファイル Write/Edit）には該当しない。判断基準に「自らプランを書き直したり分析を実行したりしない」と明記されており、副作用は workdir 内のレポート 1 本に限定される。判定キーワード（判定・点検・PASS/FAIL 相当の二値判定）が直接一致するレビュー・検証カテゴリを採用した。

## 生成本文の見出し一覧

- `# 責務`
  - `## 入力`
  - `## 出力`
- `# 判断基準`
- `# 使用するスキル`
- `# 作業手順`

`# 作業手順` を設けたのは、`## 判定` に 1 語のみを置く出力契約と、そこへ至る突き合わせ順序（完了条件ごとの対応づけ → 逸脱点検 → 判定確定 → 見出し構成に沿った Write）が本係固有の How であり、他エージェントと共有しないため（`/context-engineering` の「単一サブエージェント固有の How はサブエージェントプロンプトに書く」に従う）。

## 根拠にした設計書のセクション

- `## 作成対象サブエージェント` の `- name: analysis-evaluator` ブロック（description / 責務 / 判断基準 / 使用スキル / 入力 / 出力）— 本文の主要部はすべてここから。
- `## 全体手順（Step 1〜N）` の Step 4-3（評価）および Step 4-4（メインのループ制御）— 入力引数名と、`## 判定` を限定 Read されるという契約の根拠。
- `## 中間生成物の配置とファイル名規約` の「メインの限定 Read 契約」表 — `## 判定` に 1 語のみを置く理由（メインが同セクションだけを Read する）を本文に明記した根拠。
- `## 中間生成物の配置とファイル名規約` の `<workdir>/03_iterations/<n>_evaluation.md` 行（`## 判定` ＋ 逸脱点検 ＋ 次周フィードバック）— 生成本文の `## 完了条件の充足状況` / `## 要件逸脱の点検` / `## 次周へのフィードバック` の見出し構成の根拠。
- `## 後続工程への引き継ぎ事項`「共通情報の所在」— 外部プラグイン提供のスキル・サブエージェントを参照しない方針の根拠。

なお、命名規約そのもの（`03_iterations/<n>_evaluation.md` 等）は本係のプロンプトに書いていない。設計書が「この命名規約は生成される SKILL.md だけが知る。各サブエージェントは出力先絶対パスを引数として受け取る（疎結合）」と定めているため、本係は引数名（`evaluation-output-path`）でのみ参照している。

## 設計書独自の追記

設計書の担当ブロックに直接の記述はないが、設計書の他セクションおよび `/context-engineering` と整合する範囲で次を補った。逸脱ではなく具体化である。

- 「判定不能な完了条件」の扱い（推測で基準を補わず `CONTINUE`）— 担当ブロックの「判定は完了条件の記述にのみ基づき」を、基準が読み取れない場合に適用したときの帰結。`analysis-requirements-designer` が「完了条件は判定可能な形で書く」責務を負っている以上、判定不能な条件は要件側の不備として次周に差し戻すのが設計全体と整合する。
- 評価レポートへの機密区分の適用 — 設計書の `## 中間生成物の配置とファイル名規約` 規約「機密区分に応じて保存と掲載を制限する。要件シートで確定した区分が全工程に適用される」を本係に適用したもの。

## 設計書と /context-engineering の衝突

**1 件あり。`# 使用するスキル` セクションの内容。**

- /context-engineering（`references/subagent-prompts.md`）および agent-author の生成物構造は、サブエージェントがスキルを自動継承しないため「使用するスキルを必ず宣言する」ことを求める。
- 設計書の担当ブロックは `使用スキル: なし（本係固有の判断に閉じる）` と明記している。

**設計書を優先した。** `# 使用するスキル` セクション自体は残し、その内容を「なし」＋理由（要件シート・プラン・実行ログの 3 点の突き合わせに閉じ、他エージェントと共有する How を持たない）とした。セクションを設けたうえで「なし」と宣言することで、スキル宣言漏れとの区別がつき、かつ設計書が定めた「スキルを使わない」という構成を変えていない。したがって、生成本文には `Skill` ツール呼び出しの指示を含めていない（呼び出すべきスキルが存在しないため）。

これ以外に設計書と /context-engineering の衝突はない。
